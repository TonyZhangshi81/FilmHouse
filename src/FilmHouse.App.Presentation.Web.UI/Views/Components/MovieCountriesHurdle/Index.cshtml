@using FilmHouse.Core.ValueObjects;
@model List<SelectListItem>

@{
    <span style="color:#808080;">制片国家/地区:</span>
    foreach (var item in Model)
    {
        <a class="link-text" href="/Search/Index?country=@item.Value" target="_self">@item.Text</a>
    }
}
